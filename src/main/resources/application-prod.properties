# Configuración para entorno de producción
spring.application.name=AccountMS
server.port=8081

# MongoDB Atlas - Production
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://loana20199:V4lor4nt123$$$@cluster0.rm8bzwq.mongodb.net/accountMSdb-prod?retryWrites=true&w=majority&appName=Cluster0}

# OpenAPI/Swagger en producción (puede deshabilitarse por seguridad)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=${SWAGGER_ENABLED:false}

# Logging optimizado para producción
logging.level.org.taller01.AccountMS=INFO
logging.level.org.springframework.data.mongodb=WARN
logging.level.root=WARN

# Configuraciones de producción
spring.data.mongodb.auto-index-creation=false
server.error.include-stacktrace=never
server.error.include-message=never

# Configuración de seguridad
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
